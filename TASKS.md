# Tasks

## 当前开发准则

以后所有开发都必须先阅读：

1. `PROJECT_BRIEF.md`
2. `DESIGN_RULES.md`
3. `TASKS.md`
4. `PRODUCT.md`
5. `PRIVACY.md`

发布前必须参考：

- `RELEASE_CHECKLIST.md`

## 建议下一步

- 先把当前未发布的事件统计和文档更新提交并推送到 GitHub Pages。
- 推送后访问 `https://pictool.com.cn/`，检查页面可访问、HTTPS 正常、百度统计仍显示已安装。
- 工作台后续所有改动必须先对照四个独立页面，保持裁剪、滤镜、标题、导出配置项同步。
- 继续细化标题排版页的文本配置面板，保持紧凑和清晰层级。
- 为标题页继续检查“图片压缩”按钮和预计大小的稳定性。
- 进一步检查移动端和窄屏布局。
- 观察百度统计基础数据：实时访客、趋势分析、来源分析、受访页面、入口页面。
- 百度免费版事件分析权限有限，后续需要功能级数据时，开发自建 `/api/track`。
- 域名和 GitHub Pages 当前可继续用于小范围测试；开始推广、收费、接 AI 前规划国内云迁移和备案。

## 当前发布流程

本地修改完成后，按以下流程发布：

```bash
cd "/Users/MindsTea/Documents/Codex/2026-05-19/https-squoosh-app"
git add .
git commit -m "更新说明"
git pull --rebase origin main
git push
```

注意：

- 本机 Git 访问 GitHub 需要走代理，当前可用端口为 `http://127.0.0.1:7897`。
- 如果 `git push` 提示远端有更新，先执行 `git pull --rebase origin main`，成功后再 `git push`。
- 如果 GitHub Pages 已部署，通常等待 1-3 分钟后线上生效。
- GitHub Pages 有空间和流量限制，不适合长期承载大量素材、用户文件和 AI 结果图。

## 统计后续任务

- 当前已接入百度统计，并补充了统一事件埋点。
- 百度统计后台如果无法查看完整事件分析，不代表代码无效，可能是免费版或权限限制。
- 下一阶段建议开发自建统计接口：
  - `POST /api/track`
  - 记录访问、上传、裁剪、滤镜、标题、导出、下载等事件。
  - 继续沿用 `analytics.js` 的分桶和隐私过滤规则。
- 自建统计后台优先看这些指标：
  - 各功能打开次数
  - 图片上传次数
  - 下载完成次数
  - 裁剪预设使用量
  - 滤镜预设使用量
  - 标题添加类型
  - 工作台和单功能页转化对比

## 后端规划任务

- 第一阶段：保留当前静态前端，新增 `Node.js + Express` 后端，只做自建统计。
- 第二阶段：接 PostgreSQL，存储事件、用户、套餐、订单和 AI 任务。
- 第三阶段：接登录、会员次数、微信支付、支付宝。
- 第四阶段：接 AI 能力，包括作图、扩图、抠图。
- AI 阶段引入 Redis 做任务队列、限流和状态缓存。
- AI 上传图和结果图使用阿里云 OSS，不放 GitHub 仓库。
- 正式国内推广或收费前，迁移到阿里云/腾讯云等国内云并完成 ICP 备案。

## 回归检查清单

- 执行相关 `node --check`。
- 检查 HTML 元素引用是否和 JS 一致。
- 使用本地静态服务器预览页面。
- 检查页面高度、横向滚动、默认折叠状态。
- 重点检查标题排版页：无横向溢出、模板按钮只显示文字、4 组文字设置控件存在、主标题/副标题为多行输入。
- 重点检查工作台：裁剪预设、滤镜三组面板、标题检查器、导出压缩配置必须和独立页面一致。
- 统计相关修改后，确认各 HTML 页面仍包含百度统计代码。
- 统计相关修改后，确认 `analytics.js` 不采集文件名、标题正文、图片内容和精确图片数据。
- 部署相关修改后，确认 `CNAME` 内容仍为 `pictool.com.cn`。
