# Project Brief

## 项目目录

`/Users/MindsTea/Documents/Codex/2026-05-19/https-squoosh-app`

## 项目类型

这是一个纯静态前端项目，所有功能均在浏览器本地处理，不上传图片。

## 主要文件

- `index.html`：首页工具箱
- `styles.css`：全局样式
- `home.js`：首页占位提示逻辑
- `compress.html`：图片压缩页
- `compress.js`：图片压缩逻辑
- `crop.html`：图片裁剪页
- `crop.js`：图片裁剪逻辑
- `filter.html`：图片滤镜页
- `filter.js`：滤镜/调色逻辑
- `title.html`：标题排版页
- `title.js`：标题排版逻辑
- `workspace.html`：全屏工作台
- `workspace.js`：工作台统一编辑逻辑

## 产品方向

页面风格参考“图片工具箱”首屏：浅色背景、蓝紫主色、卡片式入口、右侧工作台。

首页包含四个功能入口：

1. 图片压缩
2. 图片裁剪
3. 图片滤镜
4. 标题排版

## 已完成模块

### 图片压缩

- 独立页面：`compress.html`
- 支持拖拽、选择、粘贴图片。
- 支持 JPEG / WebP / PNG。
- 默认导出 JPEG。
- 支持质量调节。
- 支持最大宽高设置。
- 支持宽高比例绑定。
- 支持图片内拖动对比压缩前后效果。
- 支持接收来自裁剪、滤镜、标题排版页面传来的图片，自动载入。

### 图片裁剪

- 独立页面：`crop.html`
- 支持拖拽、选择、粘贴图片。
- 支持尺寸调整和比例裁剪。
- 尺寸预设包括：
  - 自定义尺寸
  - 公众号首图 900×383
  - 公众号次图 200×200
  - 网站大图2k 1920×960
  - 网站大图4k 3840×1920
- 比例裁剪包括参考图里的比例项。
- 不包含形状蒙版、图片校正、无损改尺寸。
- 点击“应用”后，右侧预览同步显示裁剪结果。
- 支持 PNG / JPG 导出。
- 格式旁显示预计下载大小。
- 下载按钮文案为“下载图片”。
- 有“图片压缩”按钮，可把裁剪结果带入压缩页。

### 图片滤镜

- 独立页面：`filter.html`
- 支持拖拽、选择、粘贴图片。
- 右侧 canvas 实时预览效果。
- 有滤镜、基础调色、高级调色三组面板。
- 三组面板都支持折叠：
  - 默认展开“滤镜”
  - 默认收起“基础调色”
  - 默认收起“高级调色”
- 滤镜预设约 18 个。
- 基础调色包括：智能补光、亮度、对比度、饱和度、清晰度、锐化。
- 高级调色包括：
  - 光效：高光、暗部、褪色
  - 色彩：色温、色调、自然饱和
  - 细节：颗粒、暗角、怀旧
  - HSL：红色、绿色、蓝色、黑白
- 滤镜卡片高度已压缩，提高空间利用率。
- 下载区域逻辑与裁剪页一致：
  - PNG / JPG
  - 预计下载大小
  - 下载图片
  - 图片压缩

### 标题排版

- 独立页面：`title.html`
- 已按新需求重做，不再是复杂设计器。
- 不再提供“选择样式”步骤。
- 采用 3 步流程：
  1. 上传图片
  2. 点击添加文本
  3. 导出图片
- 添加文本入口包括：标题、副标题、正文。
- 用户添加文本后，在图片上真实渲染文字。
- 不让 AI 生成中文标题图片；中文标题由前端 canvas 真实文字绘制。
- 选中文本后显示精简配置项，包括内容、字体、字号、颜色、样式、对齐、间距；字体内置 10 个常用免费版权字体选项。
- 支持自动换行、主标题字数提示、标题安全区提示、JPG / PNG 导出。
- 安全区虚线只在预览中显示，导出时不带。
- 标题区域支持在画布中拖动摆放，拖动右侧手柄改变宽度，从而影响自动换行。
- 左侧菜单横向滚动已修复。

### 工作台

- 独立页面：`workspace.html`
- 使用类似 Figma 的全屏编辑器布局。
- 顶部栏包含返回、文件名、缩放和下载入口。
- 左侧工具栏紧贴屏幕边缘，包含上传、裁剪、滤镜、标题、导出。
- 左侧参数面板根据当前工具切换配置。
- 中间为统一画布预览。
- 右侧为图片信息、图层列表和导出面板。
- 当前已接入上传、裁剪尺寸/比例预设与拖动定位、滤镜预设/基础调色/高级调色、标题文字层与文字检查器、导出压缩与预计大小。
- 工作台内裁剪、滤镜、标题、导出的配置项必须以四个独立页面为准，不做简化版配置。

## 最近验证情况

多次执行过：

- `node --check`
- 页面元素引用检查
- 本地 `python3 -m http.server 4173 --bind 127.0.0.1` 预览
- 浏览器检查页面高度、横向滚动、默认折叠状态等

最近一次确认：

- `title.js` 语法通过
- `title.html` 元素引用通过
- 标题排版页无横向溢出
- 模板按钮只显示文字
- 有 4 组文字设置控件
- 主标题/副标题是多行输入
