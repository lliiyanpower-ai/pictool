# Release Checklist

## 使用范围

本清单用于图片工具箱每次发布前后的检查，尤其适用于已经上线的 `https://pictool.com.cn/`。

项目当前是 GitHub Pages 静态前端站点，基础图片处理在浏览器本地完成。发布检查重点不是后端服务健康，而是页面可访问、工具功能稳定、工作台同步、统计与隐私口径不被破坏。

## 发布前确认

### 1. 明确本次改动范围

- 确认本次改动属于哪一类：
  - 文档更新
  - 样式调整
  - 单个工具功能调整
  - 工作台功能调整
  - 统计埋点调整
  - 部署配置调整
- 使用 `git status --short` 查看工作区，避免把无关改动带入同一次提交。
- 如果工作区已有其他未提交改动，只提交本次相关文件。

### 2. 阅读必要文档

开发或发布前至少阅读：

- `PROJECT_BRIEF.md`
- `DESIGN_RULES.md`
- `TASKS.md`
- `PRODUCT.md`
- `PRIVACY.md`

涉及裁剪、智能构图、网站尺寸预设等产品方向时，优先参考 `PRODUCT.md`。

涉及统计、图片上传、本地存储、AI 云端处理、网站扫描时，必须同步检查 `PRIVACY.md`。

### 3. 保留部署关键文件

发布前确认仓库根目录保留：

- `index.html`
- `.nojekyll`
- `CNAME`

`CNAME` 内容应保持为：

```text
pictool.com.cn
```

不要把用户图片、AI 结果图、大量素材或临时文件提交到 GitHub 仓库。

## 本地检查

### 1. 语法检查

如果修改了 JavaScript 文件，执行对应文件的语法检查：

```bash
node --check analytics.js
node --check compress.js
node --check crop.js
node --check filter.js
node --check title.js
node --check workspace.js
```

只需要检查本次涉及的文件。文档-only 修改不需要执行 JS 检查。

### 2. 本地预览

涉及 HTML、CSS、JS 的改动，建议启动本地静态服务：

```bash
python3 -m http.server 4173 --bind 127.0.0.1
```

然后访问：

- `http://127.0.0.1:4173/`
- `http://127.0.0.1:4173/compress.html`
- `http://127.0.0.1:4173/crop.html`
- `http://127.0.0.1:4173/filter.html`
- `http://127.0.0.1:4173/title.html`
- `http://127.0.0.1:4173/workspace.html`

### 3. 页面基础检查

每个工具页面至少确认：

- 页面可以正常打开。
- 控制台没有明显错误。
- 上传入口可见。
- 拖拽、选择、粘贴图片的核心入口没有被破坏。
- 下载区域可见，文案保持直接。
- 页面没有明显横向溢出。
- 移动端或窄屏没有严重遮挡。

## 功能回归

### 首页

- 四个核心入口存在：图片压缩、图片裁剪、图片滤镜、标题排版。
- 工作台入口存在且可打开。
- 首页视觉风格与现有浅色工具箱风格一致。
- 不新增空壳入口。

### 图片压缩

- 支持上传图片。
- 支持 JPEG / WebP / PNG 等现有导出逻辑。
- 质量调节和最大宽高设置可用。
- 宽高比例绑定可用。
- 压缩前后预览或对比区域正常。
- 下载按钮可生成图片。
- 从其他工具传入图片时能自动载入。

### 图片裁剪

- 支持上传图片。
- 尺寸预设可选择。
- 比例裁剪可选择。
- 裁剪框可调整和拖动。
- 点击“应用”后右侧预览同步更新。
- PNG / JPG 导出可用。
- 预计下载大小显示正常。
- “图片压缩”按钮能把裁剪结果带入压缩页。

### 图片滤镜

- 支持上传图片。
- 右侧 canvas 实时预览正常。
- 滤镜、基础调色、高级调色三组面板存在。
- 默认展开和收起状态符合设计规则。
- 滤镜预设、亮度、对比度、饱和度等调整可用。
- 下载区域逻辑与裁剪页保持一致。

### 标题排版

- 流程保持为：上传图片、点击添加文本、导出图片。
- 添加文本入口包括：标题、副标题、正文。
- 选中文本后显示内容、字体、字号、颜色、样式、对齐、间距等配置。
- 文字在 canvas 中真实渲染。
- 安全区虚线只在预览中显示，导出图片不能包含安全区虚线。
- 主标题和副标题支持多行输入。
- 页面没有明显横向溢出。

### 工作台

- 工作台能正常打开。
- 上传、裁剪、滤镜、标题、导出工具可切换。
- 裁剪、滤镜、标题、当前图片导出配置必须和独立页面同类能力保持同步。
- 独立页面新增当前图片相关配置后，工作台不能保留落后版本。
- 独立页面新增批量压缩、多文件队列、ZIP 打包、网站扫描等专用工作流时，工作台不需要照搬完整流程，但应提供清晰跳转入口或说明。
- 图片处理继续在浏览器本地完成。
- 下载入口可用。

## 统计与隐私检查

### 统计入口

- `analytics.js` 仍然是统一事件入口。
- 页面代码不要散落复杂统计逻辑。
- 各 HTML 页面保留百度统计脚本和 `analytics.js` 引用。
- 新增事件命名使用稳定英文，例如 `crop_preset_selected`、`download_clicked`。

### 禁止采集

发布前确认没有新增以下采集：

- 图片内容
- 图片文件名
- 标题正文
- 用户输入文本
- 图片 Data URL
- 精确文件大小
- 完整原图尺寸
- 完整导出尺寸

需要统计大小、尺寸、质量时，必须使用分桶数据。

### 高频事件

滑杆、拖动、输入框等高频操作不能每一帧上报。只在以下稳定节点上报：

- `change`
- `blur`
- 点击应用
- 点击下载
- 工具切换
- 上传完成

## 发布操作

### 1. 提交前检查

提交前执行：

```bash
git diff --stat
git diff --cached --stat
git status --short
```

确认提交只包含本次相关文件。

### 2. 提交

提交信息应简洁描述用户可感知的变化，例如：

```bash
git commit -m "Add release and privacy docs"
```

### 3. 推送

推送前如果远端有更新，先执行：

```bash
git pull --rebase origin main
```

再执行：

```bash
git push
```

本机访问 GitHub 可能需要代理，当前项目文档中记录的可用端口为 `http://127.0.0.1:7897`。

## 线上检查

推送后通常等待 1-3 分钟，再检查线上页面。

### 访问检查

- `https://pictool.com.cn/` 可访问。
- HTTPS 正常。
- 首页能打开四个核心工具。
- 工作台能打开。
- 页面没有明显样式丢失。

### 关键路径检查

至少完成一次轻量流程：

1. 打开一个工具页。
2. 上传一张测试图片。
3. 执行一次核心操作。
4. 导出或下载结果。
5. 如涉及工作台，确认工作台同类配置同步。

### 统计检查

- 百度统计脚本仍在页面中。
- `analytics.js` 正常加载。
- 控制台没有统计相关报错。
- 如果改动涉及统计，确认事件没有携带隐私禁止字段。

## 发布后记录

发布后建议记录：

- 本次发布内容
- 涉及页面
- 是否执行本地预览
- 是否执行线上访问检查
- 是否涉及统计或隐私口径
- 是否有后续待办

如果发布后发现严重问题，优先保持 `CNAME`、`.nojekyll`、`index.html` 等部署关键文件不被破坏，再处理功能回滚或修复。
