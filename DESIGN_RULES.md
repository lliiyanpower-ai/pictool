# Design Rules

## 全局设计方向

- 使用浅色背景、蓝紫主色、卡片式入口、右侧工作台布局。
- 保持纯静态前端实现，图片处理必须在浏览器本地完成，不上传图片。
- 首页保留四个核心入口：图片压缩、图片裁剪、图片滤镜、标题排版。
- 功能页面应延续统一视觉语言和下载区域逻辑。

## 通用交互规则

- 图片类工具优先支持拖拽、选择、粘贴图片。
- 下载区域尽量保持一致：JPEG / WebP / PNG 格式选择、预计下载大小、下载图片、图片压缩。
- 跨工具传图应尽量自动载入，减少用户重复上传。
- 重要操作按钮文案保持直接，例如“下载图片”“图片压缩”。
- 所有新增工具都应先满足“可直接使用”，不要先做营销落地页或空壳入口。
- 工具型界面优先保证操作密度、清晰层级和稳定布局，避免装饰性强但影响效率的设计。

## 裁剪页规则

- 尺寸预设包含：自定义尺寸、公众号首图 900×383、公众号次图 200×200、网站大图2k 1920×960、网站大图4k 3840×1920。
- 保留参考图中的比例裁剪项。
- 不加入形状蒙版、图片校正、无损改尺寸。
- 点击“应用”后，右侧预览必须同步显示裁剪结果。

## 滤镜页规则

- 右侧使用 canvas 实时预览效果。
- 保留滤镜、基础调色、高级调色三组面板。
- 默认展开“滤镜”，默认收起“基础调色”和“高级调色”。
- 滤镜卡片应保持紧凑，提高空间利用率。

## 标题排版页规则

- 标题排版页不做复杂图层编辑，不恢复复杂设计器形态。
- 标题排版页不再提供“选择样式”步骤。
- 标题排版流程固定为 3 步：上传图片、点击添加文本、导出图片。
- 添加文本入口按参考图 1 呈现：标题、副标题、正文。
- 添加文本后显示精简配置项，不保留划重点、变形、AI花字、背景特效、尺寸、位置、排版、角度、翻转等扩展功能区。
- 中文标题必须由前端 canvas 真实文字绘制，不让 AI 生成中文标题图片。
- 页面不得出现“高校”。
- 页面不得出现“Banner”。
- 不使用标题模板列表。
- 文本内容使用多行输入，支持手动换行。
- 标题区域可在画布中拖动摆放，也可通过右侧手柄调整宽度，影响自动换行。
- 所有文字项都支持单独配置内容、字体、字号、行高、颜色、样式、对齐、间距；字体列表必须包含思源黑体、思源宋体、阿里妈妈数黑体等常用免费版权字体。
- 安全区虚线只在预览中显示，导出图片时不能出现。
- 保留主标题字数提示、标题安全区提示；不保留深色遮罩开关和对比度提示。

- 标题配置面板必须保持紧凑，字号层级遵循：一级标题、二级字段标签、三级控件文字。

## 工作台规则

- 工作台使用全屏编辑器布局，不使用首页式大卡片。
- 左侧工具栏和右侧信息/导出面板必须贴近屏幕边缘。
- 中间画布区域是主视觉，使用浅灰编辑器背景和棋盘格画布。
- 工具参数面板保持紧凑，切换工具时只显示当前工具配置。
- 工作台是单张图片的统一编辑器，重点承载当前图片的上传、裁剪、滤镜、标题和最终导出。
- 工作台必须同步独立页面中与当前图片编辑和导出直接相关的配置项、参数命名、默认值、预计大小、下载状态和隐私口径。
- 独立页面的专用工作流不要求完整搬进工作台，例如批量压缩、多文件队列、ZIP 打包、批量下载、网站扫描等。
- 独立页面新增或调整当前图片相关配置项时，必须同步检查工作台；如果新增的是专用批量或任务流能力，工作台应提供清晰跳转入口，而不是照搬完整流程。
- 工作台不能保留落后的当前图片编辑/导出能力，但可以基于单图编辑器定位保留更紧凑的适配版。
- 工作台必须继续保持浏览器本地处理，不上传图片。
- 现有四个单功能页面继续保留，工作台作为统一入口。

## 统计与隐私规则

- 统计埋点统一走 `analytics.js`，页面代码不要直接散落复杂统计逻辑。
- 百度统计脚本可以直接保留在各 HTML 页面中，确保代码安装检测通过。
- 行为事件命名使用稳定英文，例如 `image_uploaded`、`crop_preset_selected`、`download_clicked`。
- 不采集图片内容、文件名、标题正文、用户输入文本。
- 不采集精确文件大小、完整图片尺寸、完整导出尺寸；需要统计时使用区间分桶。
- 图片大小分桶可使用：`0-1m`、`1-5m`、`5-10m`、`10m+`。
- 图片尺寸分桶可使用：`small`、`medium`、`large`、`ultra`。
- 导出质量分桶可使用：`0-59`、`60-79`、`80-94`、`95-100`。
- 滑杆、拖动、输入框等高频操作不能每一帧上报；只在 `change`、`blur`、点击应用、点击下载等稳定节点上报。
- 百度统计当前主要用于访问量、来源、受访页面等基础分析；功能级事件后续应优先接自建 `/api/track`。

## 部署与域名规则

- 当前小范围测试部署使用 GitHub Pages。
- 当前正式测试域名为 `https://pictool.com.cn/`。
- GitHub Pages 仓库根目录必须保留 `index.html`、`.nojekyll` 和 `CNAME`。
- `CNAME` 内容应保持为 `pictool.com.cn`。
- 不要把用户图片、AI 结果图、大量素材放进 GitHub 仓库。
- GitHub Pages 只作为测试和早期验证方案；开始推广、收费或接 AI 后应迁移到国内云。
- 面向中国国内用户时，正式使用中国大陆服务器前必须规划 ICP 备案。

## 后端与商业化规则

- 后端接口不得把 AI 平台密钥暴露给前端。
- AI 作图、扩图、抠图等云端能力应通过后端统一封装接口。
- 基础本地图片处理和 AI 云端图片处理必须在产品文案和隐私口径上区分清楚。
- 自建统计、用户系统、会员次数、支付、AI 任务应分阶段上线，避免一次性重构全部前端。
- 首期后端建议采用 `Node.js + Express + PostgreSQL`；AI 阶段再加入 Redis 和 OSS。
