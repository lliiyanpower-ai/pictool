# Product Plan

## 产品定位

图片工具箱是一个面向网站维护者、内容运营者、新闻/科研/活动发布人员的在线图片处理工具。

产品核心目标是：让用户在发布网站内容前，可以快速完成图片压缩、裁剪、滤镜调整、标题图生成等常见处理工作，减少对复杂设计软件的依赖。

工具应优先保持轻量、直接、隐私友好。能在浏览器本地完成的图片处理，应尽量不上传服务器。

## 目标用户

主要用户包括：

- 维护学校、机构、企业、个人网站的内容管理员
- 负责新闻、科研、活动栏目更新的运营人员
- 不熟悉专业图片软件，但需要经常处理网站配图的用户
- 需要快速生成符合网站展示尺寸图片的编辑人员

## 核心原则

1. 图片处理尽量在本地浏览器完成，保护用户隐私。
2. 工具入口要简单，用户打开即用，不依赖复杂学习成本。
3. 功能设计要贴近真实网站维护场景，而不是只做通用图片编辑。
4. 裁剪、压缩、标题图生成等功能要支持预设，提高重复工作的效率。
5. 智能功能只能作为辅助，必须允许用户手动修改和确认。
6. 用户的自定义配置应优先保存在当前浏览器中，方便下次继续使用。

## 当前核心模块

### 图片压缩

用于在保证视觉质量的前提下降低图片体积，适合网站上传、新闻配图、栏目封面等场景。

### 图片裁剪

用于将图片裁剪成指定比例或尺寸，适合横幅大图、新闻封面、活动封面、科研成果图等场景。

### 图片滤镜

用于对图片进行基础视觉调整，如亮度、对比度、饱和度等。

### 标题排版

用于快速生成带标题文字的封面图、宣传图或栏目图。

### 工作台

将多个工具整合到统一入口，方便用户连续完成图片处理流程。

## 裁剪功能升级方向

裁剪工具是后续重点升级模块。

目标不是只让用户输入宽高，而是帮助用户更快得到“适合自己网站”的图片尺寸和构图结果。

### 方向一：系统预设尺寸

提供常用网站图片尺寸，例如：

- 横幅大图
- 新闻封面
- 活动封面
- 文章配图
- 头像/方图
- 社交分享图

这些预设适合新用户快速开始。

### 方向二：用户自定义尺寸

用户可以手动添加自己的常用尺寸。

每个自定义尺寸应包含：

- 名称
- 宽度
- 高度
- 所属分组
- 备注，可选

示例：

- 首页横幅：1920 x 480
- 新闻列表图：400 x 260
- 科研成果图：600 x 360
- 活动封面图：800 x 450

这些配置保存在当前浏览器中。只要用户不清理缓存，下次打开工具仍然可用。

### 方向三：网站栏目尺寸预设

用户可以以“网站”为单位维护一组图片尺寸。

使用场景：

用户维护某个网站，需要经常给不同栏目上传图片。不同栏目图片显示尺寸不同，用户希望一次记录，长期复用。

推荐流程：

1. 用户创建一个网站预设分组。
2. 输入网站名称和网站地址。
3. 手动添加栏目图片尺寸。
4. 保存为当前浏览器的默认配置。
5. 裁剪图片时，可以从“我的网站预设”中快速选择。

示例分组：

网站：某某学院官网

- 首页横幅：1920 x 520
- 新闻封面：400 x 260
- 科研动态：600 x 360
- 活动报名图：800 x 450
- 专题封面：1200 x 630

### 方向四：网站首页扫描识别

这是后续增强功能，不建议作为第一阶段直接开发。

理想效果：

用户输入自己负责维护的网站地址，工具扫描网站首页，识别页面中的图片展示区域，并推断图片用途和显示尺寸。

可能识别的信息：

- 图片所在栏目
- 图片显示宽度
- 图片显示高度
- 图片比例
- 图片附近标题或栏目名称
- 是否为横幅、新闻图、活动图等

技术限制：

如果产品保持纯前端静态站，浏览器会受到跨域限制，通常无法直接读取任意网站的页面结构和真实渲染尺寸。

如果要稳定实现自动扫描，需要后端扫描服务。后端可以使用无头浏览器打开目标网址，读取图片元素的真实展示尺寸，再返回给前端。

产品建议：

不要做完全自动保存。应先让用户确认扫描结果，再保存为网站预设。

## 智能裁剪方向

智能裁剪的目标是：用户选择尺寸后，裁剪框可以自动移动到更合理的位置，尽量保留人物或主体完整。

### 第一档：智能居中裁剪

不识别具体物体，只分析画面视觉重心。

技术思路：

- 分析图片的边缘、亮度、对比度、颜色变化等信息。
- 判断画面中信息量较高的区域。
- 根据目标比例自动选择更合适的裁剪位置。

适合场景：

- 普通风景图
- 建筑图
- 活动现场图
- 没有明显人物主体的图片

优点：

- 轻量
- 速度快
- 可纯前端实现

不足：

- 不真正理解图片内容
- 不能保证人物或主体完整

### 第二档：人物/人脸优先裁剪

识别图片中的人脸或人物位置，让裁剪框尽量保留人物完整。

适合场景：

- 新闻人物照
- 活动合影
- 团队照片
- 会议照片
- 个人头像

推荐作为智能裁剪的重点版本。

产品表现：

- 检测到人物时，裁剪框自动靠近人物区域。
- 如果检测到多个人，优先保证主要人物或尽量覆盖多人。
- 如果目标尺寸过窄，无法完整保留所有人，应给出提示。
- 用户始终可以手动拖动裁剪框。

提示文案示例：

- 已优先保留人物区域
- 当前比例较窄，可能无法完整保留所有人物
- 未检测到明显人物，已使用智能构图

### 第三档：通用主体识别

识别人以外的主体，例如产品、建筑、设备、车辆、科研仪器、图表等。

适合更高级的智能裁剪。

技术思路：

- 使用对象检测或图像分割模型识别主体。
- 判断多个主体中的主要对象。
- 自动移动裁剪框，保证主体完整并保持构图自然。

优点：

- 适用范围更广
- 智能体验更强

不足：

- 模型更重
- 移动端性能压力更大
- 误识别概率更高
- 需要更完整的交互兜底

## 推荐阶段计划

### V1：自定义裁剪尺寸

目标：

让用户可以保存自己的常用裁剪尺寸。

功能包括：

- 添加自定义尺寸
- 编辑自定义尺寸
- 删除自定义尺寸
- 按名称快速选择尺寸
- 配置保存在当前浏览器中

验收标准：

- 用户刷新页面后，自定义尺寸仍然存在。
- 用户可以在裁剪时直接选择自定义尺寸。
- 删除尺寸后，不再出现在预设列表中。

### V2：网站栏目尺寸预设

目标：

让用户以网站为单位管理一组栏目图片尺寸。

功能包括：

- 创建网站预设分组
- 填写网站名称和网址
- 添加多个栏目尺寸
- 在裁剪时按网站分组选择尺寸
- 支持编辑和删除网站预设

验收标准：

- 一个网站下可以保存多个栏目尺寸。
- 用户可以快速切换不同网站的预设。
- 配置保存在当前浏览器中。

### V3：智能构图与人物优先裁剪

目标：

让裁剪框在选择尺寸后自动移动到更合理的位置。

功能包括：

- 智能构图开关
- 默认视觉重心裁剪
- 检测到人物时优先保留人物
- 无法完整保留时给出提示
- 用户可继续手动调整裁剪框

验收标准：

- 开启智能构图后，裁剪框不只是机械居中。
- 人物图片中，裁剪框应尽量保留人物脸部和身体主体。
- 用户手动调整后，不应被系统反复自动改回。
- 图片不上传服务器。

### V4：网站首页扫描识别

目标：

帮助用户自动发现网站中的图片展示尺寸。

功能包括：

- 输入网站地址
- 扫描首页图片区域
- 返回疑似栏目名称和图片显示尺寸
- 用户确认、修改后保存为网站预设

验收标准：

- 扫描结果必须经过用户确认。
- 用户可以删除误识别项。
- 用户可以修改栏目名称和尺寸。
- 扫描失败时，应给出清晰提示。

### V5：通用主体识别裁剪

目标：

支持人物以外的主体识别，让裁剪适用于更多图片类型。

功能包括：

- 识别图片主要主体
- 保证主体尽量完整
- 多主体时提供合理优先级
- 保留手动调整能力

验收标准：

- 对常见产品图、建筑图、设备图有明显改善。
- 模型加载速度可接受。
- 移动端不应明显卡顿。
- 识别失败时可回退到普通智能构图。

## 数据保存建议

第一阶段建议使用浏览器本地存储保存用户配置。

可保存内容：

- 自定义尺寸
- 网站预设分组
- 栏目尺寸
- 最近使用的尺寸
- 智能构图开关状态

注意事项：

- 应提示用户：配置保存在当前浏览器中，清理缓存后可能消失。
- 后续如果需要多设备同步，再考虑账号系统或云端保存。

## 隐私原则

图片处理功能应优先保持本地处理。

产品表达中可以强调：

- 图片不上传服务器
- 裁剪、压缩等处理在浏览器本地完成
- 用户的网站尺寸配置保存在本机浏览器中

如果未来加入网站扫描服务，应明确说明：

- 扫描的是用户输入的网站首页
- 不扫描后台页面
- 不读取用户本地图片
- 扫描结果需用户确认后保存

## 近期优先级建议

优先做：

1. 自定义裁剪尺寸
2. 网站栏目尺寸预设
3. 本地保存用户配置
4. 智能构图开关
5. 人物/人脸优先裁剪

暂缓做：

1. 自动扫描任意网站
2. 通用主体识别
3. 账号系统和云同步
4. 复杂批量智能裁剪

## 产品判断

裁剪工具后续不应只做“输入宽高再裁剪”，而应升级为“网站配图裁剪助手”。

它的核心竞争力可以是：

- 记住用户网站的常用图片尺寸
- 帮用户快速选对栏目尺寸
- 自动把人物或主体放进合适位置
- 保持本地处理和隐私友好
- 让非设计人员也能稳定产出合适的网站配图
