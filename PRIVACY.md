# Privacy Notes

## 文档定位

本文档用于统一图片工具箱的隐私与数据处理口径，指导后续功能设计、开发和发布检查。

它是项目内部产品与开发约束，不替代正式法律隐私政策。未来如果产品开始大规模推广、接入账号、支付、AI 云端能力或面向企业客户，应再准备正式对外隐私政策和用户协议。

## 核心承诺

图片工具箱的基础能力应优先保持本地处理：

- 图片压缩在浏览器本地完成。
- 图片裁剪在浏览器本地完成。
- 图片滤镜在浏览器本地完成。
- 标题排版在浏览器本地完成。
- 基础导出在浏览器本地完成。

默认情况下，用户上传到工具中的图片不应上传到服务器。

## 当前数据处理方式

### 图片文件

用户选择、拖拽或粘贴的图片，仅用于当前浏览器页面内的处理和预览。

当前基础工具不应采集：

- 图片内容
- 原始图片文件
- 导出后的图片文件
- 图片文件名
- 图片中的文字内容
- 用户输入到图片上的标题正文

### 浏览器本地数据

后续功能可以使用浏览器本地存储保存用户配置，例如：

- 自定义裁剪尺寸
- 网站栏目尺寸预设
- 最近使用的尺寸
- 智能构图开关状态
- 常用导出配置

这些配置应保存在当前浏览器中。用户更换浏览器、使用无痕模式或清理缓存后，配置可能丢失。

产品文案应避免暗示这些配置已经云端同步。

### 统计数据

当前统计入口是 `analytics.js`。

允许统计的内容包括：

- 页面访问
- 工具打开
- 图片上传成功或失败
- 裁剪预设选择
- 裁剪比例选择
- 应用裁剪
- 滤镜预设选择
- 调色项调整
- 标题类型添加
- 导出格式选择
- 下载点击
- 压缩跳转
- 工作台工具切换
- 智能构图开关开启或关闭
- 重新智能构图点击
- 智能裁剪成功或失败

统计字段应尽量使用分桶或枚举值，而不是精确用户数据。

允许使用的分桶示例：

- 图片大小分桶：`0-1m`、`1-5m`、`5-10m`、`10m+`
- 图片尺寸分桶：`small`、`medium`、`large`、`ultra`
- 导出质量分桶：`0-59`、`60-79`、`80-94`、`95-100`

## 禁止采集的数据

除非未来经过明确产品设计、用户提示和合规评估，否则禁止采集：

- 图片内容
- 图片文件名
- 图片 Data URL
- 图片二进制数据
- 标题正文
- 用户输入文本
- 精确文件大小
- 完整原图尺寸
- 完整导出尺寸
- 用户本地路径
- 用户剪贴板中的非图片内容
- 用户网站后台地址、账号或密码

如果后续接入自建统计接口，也必须继续遵守以上限制。

## 埋点设计规则

### 统一入口

所有行为统计应通过 `analytics.js` 中的统一方法发送：

- `trackEvent()`
- `trackToolEvent()`

页面和工具脚本不应各自实现分散的统计上报逻辑。

### 字段清洗

上报前应过滤敏感字段。

当前应避免使用或透传以下字段名：

- `text`
- `fileName`
- `filename`
- `file_name`
- `name`
- `dataUrl`
- `x`
- `y`
- `width`
- `height`
- `face`
- `faces`
- `person`
- `people`
- `box`
- `boxes`
- `coordinates`
- `file_size_mb`
- `output_size_mb`
- `output_width`
- `output_height`

需要统计大小、尺寸或质量时，使用分桶结果。

智能裁剪埋点只允许上报：

- 策略类型：`face`、`person`、`saliency`、`center`
- 结果状态：`success`、`failed`
- 图片尺寸分桶和目标尺寸分桶

不得上报人脸坐标、人物坐标、检测框数量、检测截图或精确尺寸。

### 高频操作

滑杆、拖动、输入框等高频交互不能每一帧上报。

推荐只在以下节点上报：

- 用户完成选择
- 输入框失焦
- 点击应用
- 点击下载
- 上传完成
- 工具切换完成

## 本地预设与网站尺寸功能

后续裁剪功能可能支持：

- 自定义裁剪尺寸
- 以网站为单位保存栏目图片尺寸
- 记录最近使用的尺寸

这些数据默认应保存在当前浏览器中。

建议向用户说明：

```text
这些预设保存在当前浏览器中，清理缓存或更换浏览器后可能丢失。
```

如果未来支持账号和云端同步，应在上线前重新评估：

- 保存哪些字段
- 是否保存网站地址
- 是否支持用户删除
- 是否支持导出或迁移
- 是否需要单独开关

## 智能裁剪功能

智能裁剪应优先在浏览器本地完成。

可能使用的本地分析包括：

- 视觉重心分析
- 人脸检测
- 人物检测
- 主体识别

产品原则：

- 用户图片默认不上传。
- 智能结果只作为建议。
- 用户必须可以手动调整裁剪框。
- 识别失败时应回退到普通裁剪或智能居中。

推荐提示文案：

```text
智能构图在当前浏览器中完成，图片不会上传。
```

如果未来某些 AI 能力需要云端处理，必须明确区分为云端能力，并在用户操作前提示。

## 网站扫描功能

后续可能支持用户输入自己维护的网站地址，扫描首页图片展示尺寸，并保存为网站栏目尺寸预设。

该能力与本地图片处理不同，可能需要后端扫描服务。

上线前必须明确：

- 扫描的是用户主动输入的网站地址。
- 默认只扫描公开首页或公开页面。
- 不扫描网站后台。
- 不要求用户输入网站后台账号或密码。
- 不读取用户本地图片。
- 扫描结果必须让用户确认后再保存。

扫描结果可包含：

- 网站名称
- 网站地址
- 疑似栏目名称
- 图片显示宽度
- 图片显示高度
- 图片比例

扫描结果不应自动保存为用户配置，必须经过用户确认。

## 未来 AI 云端能力

如果后续加入以下功能，可能需要上传图片到服务器或第三方 AI 服务：

- AI 作图
- AI 扩图
- AI 抠图
- AI 图片修复
- AI 通用主体识别云端版

这些功能上线前必须满足：

- 与本地基础工具在产品文案上明确区分。
- 用户操作前明确提示图片将上传处理。
- 不把 AI 平台密钥暴露给前端。
- 上传图和结果图不放入 GitHub 仓库。
- 明确图片保存时间、删除机制和访问权限。
- 重新检查正式隐私政策和用户协议。

推荐提示文案方向：

```text
此功能需要将图片上传到云端处理。基础压缩、裁剪、滤镜和标题排版仍可在浏览器本地完成。
```

## 第三方服务

当前项目已接入百度统计，用于基础访问数据和事件分析。

使用第三方统计时应遵守：

- 不上报图片内容。
- 不上报文件名。
- 不上报标题正文。
- 不上报精确图片数据。
- 不把第三方统计脚本扩展成业务数据存储。

如果未来接入更多第三方服务，应先在本文档中补充数据边界。

## 发布检查要求

涉及以下改动时，必须同步检查本文档：

- 新增统计事件
- 修改 `analytics.js`
- 新增本地存储
- 新增网站扫描
- 新增 AI 处理能力
- 新增后端接口
- 新增账号、会员、支付
- 新增云端保存或同步

发布前必须确认：

- 图片是否仍然本地处理。
- 是否新增了上传行为。
- 是否新增了隐私敏感字段。
- 是否需要用户确认或提示。
- 是否需要更新对外说明。

## 产品表达建议

可在产品页面或帮助说明中使用以下口径：

```text
基础图片处理在浏览器本地完成，图片不会上传服务器。
```

```text
自定义预设保存在当前浏览器中，清理缓存后可能丢失。
```

```text
涉及云端 AI 处理的功能会在使用前单独提示。
```
